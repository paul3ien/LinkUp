# Apply all LinkUp Kubernetes manifests
# Usage: kubectl apply -k k8s/
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: linkup

resources:
  - namespace.yaml
  - postgres-statefulset.yaml
  - deployment.yaml
  - frontend.yaml
  - ingress.yaml
